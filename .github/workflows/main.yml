name: Jest Unit Tests

on: 
  push:
    branches: 
    - main
    

jobs:
  Jest:
    runs-on: windows-latest
    steps: 
    - uses: actions/checkout@v1
    - name: Run Tests
    run: |
        npm install
        npm test